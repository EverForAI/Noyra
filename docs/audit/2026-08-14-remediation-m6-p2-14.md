# Noyra P2-14 修复复核

日期：2026-08-14  
范围：因果时间异常只在实时写入时保护，历史导入异常没有可见报告。

## 修复内容

`EventStore.causal_anomalies()` 扫描同一主体的因果边，报告父事件
`occurred_at` 晚于子事件的历史 anomaly，最多保留有限样本。Resilience audit 将数量、
样本 ID 和时间写入报告，并把存在异常标记为 P1，而不是修改或拒绝历史导入。

实时事件原有的“父事件不能晚于子事件”写入保护保持不变。

## 验证

* 新增历史导入 anomaly 检测测试；
* 事件、韧性、全量测试、静态检查和部署专项审计在提交前执行。
